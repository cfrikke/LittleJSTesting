'use strict';

const tileType_empty = 0;
const tileType_soild = 1;

let player, playerStartPos, tileLayer, tileBackgroundLayer, gameTimer = new Timer;
let levelSize, levelColor, levelGroundColor, warmup;
